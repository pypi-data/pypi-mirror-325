[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "socaity"
version = "0.0.2"
description = "Your SDK and model zoo for generative AI."
requires-python = ">=3.8"
authors = [
    { name = "SocAIty" }
]
license = { text = "GPLv3" }
readme = "README.md"
dependencies = [
    "tqdm",
    "httpx",
    "fastsdk>=0.0.7",
    "media-toolkit>=0.1.8",
    "singleton_decorator==1.0.0"
]

[project.urls]
Repository = "https://github.com/SocAIty/socaity"
Homepage = "https://www.socaity.ai"
