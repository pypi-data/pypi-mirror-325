[project]
name = "{{folder_name}}"
version = "0.1.0"
description = "{{name}} using peepsAI"
authors = [{ name = "Your Name", email = "you@example.com" }]
requires-python = ">=3.10,<3.13"

[project.scripts]
kickoff = "{{folder_name}}.main:kickoff"
plot = "{{folder_name}}.main:plot"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.peepsai]
type = "flow"
