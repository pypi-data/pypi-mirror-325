[project]
name = "nephyx"
version = "0.0.2"
description = "nephyx quick project setup for FastAPI applications"
authors = [
    { name = "Sascha Lamp", email = "sascha.lamp@lampware.de" },
]
dependencies = [
    "sqlalchemy>=2.0.37",
    "fastapi>=0.115.7",
    "fastapi-utils>=0.8.0",
    "fastapi-utilities>=0.3.0",
]
requires-python = ">=3.12"
readme = "README.md"

[project.license]
text = "MIT"

[build-system]
requires = [
    "pdm-backend",
]
build-backend = "pdm.backend"

[tool.pdm]
distribution = true
