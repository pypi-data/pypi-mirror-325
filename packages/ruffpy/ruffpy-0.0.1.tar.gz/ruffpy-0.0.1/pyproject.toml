[project]
name = "ruffpy"
version = "0.0.1"
description = "A ruff clone in python"
authors = [
    { name = "Mathieu Dupuy", email = "deronnax@gmail.com" },
]
dependencies = []
requires-python = ">=3.8"
readme = "README.md"

[project.license]
text = "MIT"

[build-system]
requires = [
    "pdm-backend",
]
build-backend = "pdm.backend"

[tool]
