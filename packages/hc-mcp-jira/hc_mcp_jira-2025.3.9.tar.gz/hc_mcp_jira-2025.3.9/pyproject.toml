[project]
name = "hc-mcp-jira"
version = "2025.3.9"
description = "HC MCP server for Jira integration"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.2.0",
    "jira>=3.5.1",
    "mcp_config_manager>=0.1.1",
]
[[project.authors]]
name = "Erik Meinders"
email = "erik@easytocloud.com"

[build-system]
requires = [ "hatchling",]
build-backend = "hatchling.build"

[project.scripts]
hc-mcp-jira = "hc_mcp_jira:main"
