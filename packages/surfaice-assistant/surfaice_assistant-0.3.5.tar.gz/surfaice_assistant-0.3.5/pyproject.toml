[virtualenvs]
in-project = true

[tool.poetry]
name = "surfaice-assistant"
version = "0.3.5"
description = ""
authors = ["Zhanibek <rzhanik@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
openai = "^1.52.0"
asyncio = "^3.4.3"
httpx = "^0.27.2"


[tool.poetry.group.dev.dependencies]
flake8 = "^7.1.1"
isort = "^5.13.2"
pytest = "^8.3.3"
pytest-asyncio = "^0.24.0"
pytest-env = "^1.1.5"
pytest-mock = "^3.14.0"
pytest-cov = "^5.0.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.pytest.ini_options]
env = [
    "OPENAI_API_KEY=secret",
]
