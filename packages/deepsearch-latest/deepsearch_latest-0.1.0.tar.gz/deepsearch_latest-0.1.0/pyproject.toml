[project]
name = "deepsearch-latest"
version = "0.1.0"
description = "Deep search tool for document analysis"
dependencies = [
    "httpx",
    "aiohttp",
    "pandas",
    "numpy",
    "matplotlib",
    "tiktoken",
    "python-docx",
    "pdf2image",
    "Pillow",
    "boto3",
    "pinecone-client",
    "python-dotenv",
    "fastmcp"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["deepsearch"]