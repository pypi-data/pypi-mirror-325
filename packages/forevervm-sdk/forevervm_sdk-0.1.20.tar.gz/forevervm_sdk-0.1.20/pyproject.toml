[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "forevervm-sdk"
version = "0.1.20"
dependencies = [
    "httpx>=0.28.1",
    "httpx-ws>=0.7.1",
]
description = "Developer SDK for foreverVM"
readme = "README.md"

[dependency-groups]
dev = [
    "pytest>=8.3.4",
]
