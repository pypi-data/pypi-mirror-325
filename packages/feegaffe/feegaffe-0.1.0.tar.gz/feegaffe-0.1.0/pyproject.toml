[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "feegaffe"
version = "0.1.0"
authors = [{name = "Fée Gaffe", email = "contact@feegaffe.fr"}]
description = "Une librairie simple pour dire Hello"
readme = "README.md"
license = {file = "LICENSE"}
dependencies = []
