from setuptools import setup, find_packages

setup(name='pyvp',
      version='1.0.0',
      description='none',
      packages=['pyvp'],
      author_email='',
      install_requires=['pyperclip','IPython'],
      zip_safe=False)
