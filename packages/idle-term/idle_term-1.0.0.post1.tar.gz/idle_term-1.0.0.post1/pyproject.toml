[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "idle-term"
version = "1.0.0.post1"
dependencies = ["psutil", "hypno"]
description = "A library providing some terminal features for IDLE's shell"
readme = "README.md"
keywords = ["idle", "terminal", "progressbar"]

[project.urls]
Homepage = "https://github.com/pyhacks/idle-term"