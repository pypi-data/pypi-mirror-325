def to_binary(num):
    binary = bin(num)[2:]
    return binary


def to_decimal(num, base):
    decimal = int(num, base)
    return decimal


def to_hexadecimal(num):
    hexadecimal = hex(num)[2:]
    return hexadecimal

